name: Code quality

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

env:
  PYTHONPATH: src
  PYTHON_VERSION: "3.10"

jobs:
  unit-quality:
    name: Validate Python code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: Upgrade pip and install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Check import order with isort
        run: |
          python -m isort --check-only src test 

      - name: Check formatting with black
        run: |
          python -m black --check src test 

      - name: Check PEP8 with flake8
        run: |
          python -m flake8 src test

      - name: Run unit tests with coverage
        run: |
          python -m pytest "test/mlops/" \
            --cov=src/simarro/mlops \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-report=html \
            --cov-fail-under=60

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-reports
          path: |
            coverage.xml
            htmlcov/

  integration-tests:
    name: Integration tests with Docker
    runs-on: ubuntu-latest
    needs: unit-quality

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Upgrade pip and install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Verify Docker Compose availability
        run: docker compose version

      - name: Start MLOps stack
        run: |
          cp .env.example .env
          docker compose up --wait

      - name: Run integration tests
        run: |
          python -m pytest test/integration/mlops -m integration

      - name: Stop Docker stack
        if: always()
        run: |
          docker compose down -v
